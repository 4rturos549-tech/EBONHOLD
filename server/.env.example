# Copia este archivo a .env y rellena
# Compose lo lee automaticamente.

# Secreto compartido entre el bridge y Vercel.
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BRIDGE_KEY=cambiame-genera-uno-con-crypto-randomBytes-32-hex
